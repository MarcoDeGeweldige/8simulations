using UnityEngine;

public class DetectIonLoop : MonoBehaviour
{
    private CarSensormsg Sensors = new CarSensormsg();

    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}